# Test-only appender key. Provides a known X-Api-Key so full-stack @SpringBootTest contexts start
# (the security layer fails closed when no key is configured) and append tests can authenticate.
# Loaded alongside the main application.yml (different filename → both are read, properties merged).
etchlog.security.api-keys=test-api-key
